#include<stdio.h>
int main(){
char ch;
printf("enter the character :");
scanf("%c",&ch);

if(ch>='A' && ch<='Z'){
    printf("upper case");
}
    else if(ch>='a' && ch<='z'){
        printf("lower case");
    }
    else{
        printf("not a english character");
    }
}
//for loop(simple code)
#include<stdio.h>
int main(){
    
    for (int i=0;i<=10;i=i+1){
        printf("%d\n",i);
    
    }
    return 0;
}
